{
  "_phase": "1 — file 1 of 7",
  "_todo_after_this_file": [
    "Run from repo root: npm install",
    "Then create tsconfig.json (say 'next step')",
    "Then schema.sql → src/index.ts → src/types.ts → src/migrate.ts"
  ],
  "_name_note": "Workspace package imported as @crono/db in backend later",
  "name": "@crono/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "_scripts_note": "build before migrate; migrate runs compiled dist/migrate.js",
  "scripts": {
    "build": "tsc",
    "migrate": "node dist/migrate.js"
  },
  "_dependencies_note": "pg = Postgres client pool; dotenv = load repo root .env in migrate/index",
  "dependencies": {
    "dotenv": "^16.4.5",
    "pg": "^8.13.0"
  },
  "_devDependencies_note": "TypeScript + types for Node and pg",
  "devDependencies": {
    "@types/node": "^20.17.0",
    "@types/pg": "^8.11.10",
    "typescript": "^5.6.3"
  }
}

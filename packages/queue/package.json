{
  "_phase": "2 — file 1 of 4",
  "_todo_after_this_file": [
    "Run from repo root: npm install",
    "Then create tsconfig.json (already scaffolded if you used next step)",
    "Then src/redis.ts → src/index.ts → npm run build -w @crono/queue"
  ],
  "_name_note": "Workspace package imported as @crono/queue in backend + worker later",
  "name": "@crono/queue",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc"
  },
  "_dependencies_note": "bullmq = job queue; ioredis = Redis client; dotenv = load repo root .env",
  "dependencies": {
    "bullmq": "^5.34.0",
    "dotenv": "^16.4.5",
    "ioredis": "^5.10.1"
  },
  "_devDependencies_note": "TypeScript + Node types only",
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.6.3"
  }
}
